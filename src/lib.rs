#![feature(const_trait_impl)]

pub mod life;
pub mod starsystem;
mod util;
pub mod config;
pub mod html;

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
